[pytest]
# Place this file at the project root (same level as pyproject.toml)
testpaths = tests
pythonpath = .
addopts =
    -v
    --tb=short
    --strict-markers
markers =
    unit:        Pure logic, no I/O
    integration: Full HTTP + real DB (in-memory SQLite)
    regression:  Guards against previously fixed bugs